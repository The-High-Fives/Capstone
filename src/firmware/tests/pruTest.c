#include "../utils.h"

int main()
{
    drawCircle(300, 100, 30, 1);
    drawRect(300, 300, 50, 50, 2);
    // int i, iter = 0;
    // do
    // {
    //     iter = (iter + 1) & 7;

    //     int x = 80 + (iter & 1) * 160;
    //     int y = 80 + (iter & 2) * 80;

    //     if (iter & 4)
    //     {
    //         drawCircle(x, y, 10, 1);
    //     }
    //     else
    //     {
    //         drawRect(x, y, 10, 10, 2);
    //     }

    //     for (i = 0; i < 10000000; i++)
    //     {
    //     }

    // } while (1);

end:
    goto end;
}
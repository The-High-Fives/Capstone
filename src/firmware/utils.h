#pragma once

#include "defs.h"

void drawCircle(int x, int y, int radius, Color color);
void drawRect(int x, int y, int width, int height, Color color);
void drawSprite(int x, int y, int memoryLocation, int wordCount, Color color);